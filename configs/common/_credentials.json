{
  "forum": {
    "auth": {
      "api-tokens": [
        "A list of github access tokens, it is necessary for the github API calls while the user is not logged in"
      ],
      "admin-token": "Github user access token which has the rights to push to the repository which stores the issues for the set of the label when creating/editing the post",
      "secret": "Secret word for session middleware"
    }
  }
}
