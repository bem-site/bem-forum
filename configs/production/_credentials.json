{
    "NODE_ENV": "production",
    "forum": {
        "oauth": {
            "{host}": {
                "clientId": "your client id for your {host}",
                "clientSecret": "your secret key for your {host}",
                "redirectUrl": "your redirect url for your {host}"
            }
        }
    }
}
