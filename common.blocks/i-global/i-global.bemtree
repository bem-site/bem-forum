this._mode === '' && !this.data: {
    return apply(
        this._mode = '',
        this.data = this.ctx,
        this.ctx = {
            block: 'page',
            title: 'Bem Forum'
        }
    );
}