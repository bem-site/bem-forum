this._mode === '' && !this.data: {
    return apply(
        this._mode = '',
        this.data = true,
        this.ctx = {
            block: 'page',
            title: 'Bem Forum'
        }
    );
}
