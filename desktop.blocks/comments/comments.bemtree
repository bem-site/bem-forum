block('comments')(
    match(function() { return req.cookies['forum_username'] && !this._isArchive; }).content()(function() {
        return [
            applyNext(),
            {
                elem: 'add-form'
            }
        ]
    })
);
