block('comments')(
    match(function () { return this._forum.user && !this._isArchive; }).content()(function () {
        return [
            applyNext(),
            { elem: 'add-form' }
        ];
    })
);
