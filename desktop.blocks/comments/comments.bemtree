block('comments')(
    match(function() { return req.cookies['forum_username']; }).content()(function() {
        return [
            !this._forum.comments ? applyNext() : undefined,
            {
                elem: 'add-form'
            }
        ]
    })
);
