block('comments')(
    match(function () { return this._forum.user && !this._isArchive; }).content()(function () {
        return [
            (!this._forum || !this._forum.comments) ? applyNext() : undefined,
            { elem: 'add-form' }
        ]
    })
);
