block('comments').elem('add-form')(

    tag()('form'),

    def()(function() {
        applyCtx({
            block: 'form',
            mods: { action: 'comments' },
            mix: [{ block: 'comments', elem: 'add-form' }, { block: 'flex' }],
            content: this.ctx.content
        });
    })
);