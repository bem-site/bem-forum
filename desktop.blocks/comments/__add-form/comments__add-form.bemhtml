block('comments').elem('add-form')(

    tag()('form'),

    def()(function() {
        applyCtx({
            block: 'forum-form',
            mix: [{ block: 'comments', elem: 'add-form' }, { block: 'forum-flex' }],
            content: this.ctx.content
        });
    })
);
