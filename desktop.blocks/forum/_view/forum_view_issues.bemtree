block('forum').mod('view', 'issues')(

    content()(function() {
        return [
            {
                block: 'issues',
                issues: this.ctx.data
            }
        ];
    })
);
