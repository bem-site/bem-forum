block('page')(

    def()(function () {
        this.ctx.head = applyCtx({ elem: 'css' });

        return applyNext(this.ctx);
    }),

    content()([
        { block: 'forum' },
        { elem: 'js' }
    ])
);
