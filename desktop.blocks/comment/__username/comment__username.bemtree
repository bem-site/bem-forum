block('comment').elem('username')(
    def()(function() {
        return {
            block: 'forum-user',
            elem: 'name',
            url: this.ctx.url,
            mix: [{ block: 'comment', elem: 'username' }],
            content: this.ctx.name
        };
    })
)
