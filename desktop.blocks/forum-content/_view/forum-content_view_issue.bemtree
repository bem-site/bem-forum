block('forum-content').mod('view', 'issue')(
    content()(function () {
        var forum = this._forum,
            config = forum.config;

        return {
            block: 'issue',
            data: forum.issue,
            forumUrl: config.url,
            labelsRequired: config.labelsRequired,
            csrf: forum.csrf
        }
    })
);
