block('forum-content').mod('view', 'issue')(
    content()(function() {
        return {
            block: 'issue',
            issue: this._forum.issue,
            js: { forumUrl: req.forumUrl }
        }
    })
);
