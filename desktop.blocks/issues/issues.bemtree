block('issues')(
    content().match(this.ctx.issues)(function() {
        var issues = this.ctx.issues;

        return issues.map(function(issue) {
            return {
                block: 'issue',
                issue: issue
            }
        });
    })
);
